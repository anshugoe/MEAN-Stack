doctype html
html
  head
    meta(charset='utf-8')
  body
    fieldset
      form(action='/hw3', method='post')
        input.ghost-input(name='city', type='text', placeholder='Enter the City', required='')
        input.ghost-button(type='submit', value='Get Coordinates')
      if state
        p= state
      if error
        p= error